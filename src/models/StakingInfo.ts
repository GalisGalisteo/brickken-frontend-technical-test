export type StakingInfo = {
  projectedAmount?: string;
  depositedAmount?: string;
  isUserStaker?: boolean;
  bknAmount?: string;
  roi?: string;
  roiSeconds?: string;
  isDepositable?: boolean;
  isClaimable?: boolean;
  withdrawableUserBalance?: string;
};
